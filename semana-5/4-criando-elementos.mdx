
Criando elementos
==================

Não basta apenas destruir. É preciso também criar. Por isso hoje vamos aprender como dar origem a novos elementos HTML em nossas páginas usando javascript.


👵🏻 Vamos lá!
---------------

Para criar novos elementos na página vamos usar um pouco do conhecimento que ja temos:

```javascript
<div id="app">
<span id="frase"></span>
</div>

<script>
var Elemento = document.getElementById('frase');
var texto = document.createTextNode("Uma ótima tarde a todos");
Elemento.appendChild(texto);
</script>
```
A novidade aqui são as duas ultimas linhas. Vamos prestar bastante atenção a elas.

* com **createTextNode** criamos um novo elemento de texto html
* com **appendChild** incluimos o texto dentro do Elemento definido acima.

Incluindo um elemento em outro
-------------------------------

O exemplo acima pode melhorar para incluirmos além do texto uma tag h1:

```javascript
<div id="app">
<span id="frase"></span>
</div>

<script>
var Elemento = document.getElementById('frase');
var ElementoH1 = document.createElement("h1");
var texto = document.createTextNode("Uma ótima tarde a todos");

ElementoH1.appendChild(texto);
Elemento.appendChild(ElementoH1);

</script>
```
O exemplo acima cria uma nova tag h1.. em seguida inclui dentro dela o texto e depois inclui tudo isso na tag com id frase.

Exercicio
===========

Crie uma tag h2. Ela deve conter o seu nome e aparecer abaixo da tag h1 criada no script acima.
